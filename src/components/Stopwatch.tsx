export default function Stopwatch() {
  // TODO(phase-6): timestamp-based state (startedAt, accumulatedMs, running)
  // + rAF-driven display refresh. Component stays mounted across tab switches.
  return <div className="stopwatch">00:00</div>;
}
